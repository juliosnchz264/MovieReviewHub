/**
 * Placeholder. Populated by `pnpm api:types` which reads Swagger from a
 * running backend (default http://localhost:8080/v3/api-docs).
 *
 * DO NOT commit a generated payload here unless you also wire CI to keep it
 * in sync — drift silently between codegen runs is worse than no codegen.
 *
 * Until codegen is wired into CI, keep using the hand-maintained `src/types/*`
 * and reach for `paths` / `components` from this file only for endpoints
 * you've just regenerated locally.
 */
export type _PendingCodegen = never;
